export class Servicios{

    nombre:string;
    disponible:boolean;

    constructor(nombre:string, disponible:boolean){
        
        this.nombre = nombre;
        this.disponible = disponible;
}
}